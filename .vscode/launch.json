{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "zhiz_cmake_launch",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}", // 执行哪个文件
      "args": [],
      "stopAtEntry": false,
      "cwd": "${command:cmake.launchTargetDirectory}", // 相对路径解析从哪里开始
      "environment": [],
      "console": "integratedTerminal",
      // "preLaunchTask": "compile current C++ file"
    }
  ]
}
