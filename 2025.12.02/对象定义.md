---
title: 对象定义
date: 2025.12.02
tags: [cpp, practice]
---

# 对象定义

## 代码说明

（这里写这段代码要解决什么问题、关键点是什么）

## 学习笔记

（记录遇到的坑、思路变化、调试过程和总结）

### 1 对象
- 对象是某个类型的一块内存区域
- 对象具有大小、对齐、存储期、生命周期和值等属性。

### 2 存储期
***存储期**
- 针对内存的概念，对象的一个属性，定义”包含该对象的那块内存“的最小潜在生命周期。
- 由对象的创建方式决定。

***存储期分为**
- 静态存储期
    - 生命周期从程序启动前开始，到程序结束时结束。
    - 全局变量
    - 命名空间作用域
    - static 关键字命名的变量

- 线程存储期
    - 使用 thread_local 关键字命名，生命周期从线程的分配到停止。
    - 猜测线程存储期停止有2种方法：
        - 1，定义线程的时候设定终止条件
        - 2，使用代码控制终止

- 自动存储期
    - 执行流从{进入代码块，这时分配内存，从}终止生命周期。
    - 取决于源代码中的{}范围
    - 分配「栈」

- 动态存储期
    - 分配 「堆」
    - 使用new关键字定义，生命周期由delete关键字结束。

### 3 存储期、生命周期和作用域的区别

- 存储期：对象的一个属性，定义“该对象所在那块内存”的最小潜在生命周期。
存储期针对的是内存的分配和释放的描述。
    - 对某一次「对象使用存储」的过程，我们从「给这个对象准备内存」到「最终释放这块内存」，
    这段时间就叫这个对象的存储期。
    - 存储期由对象而生，然后对象不在了它还可能在。

- 生命周期：T对象在内存中的存在时间。
    - 生命周期从时间上被包含于存储期
    - 读取生命周期之外的某类型对象是未定义行为

- 作用域：编译期的概念，源代码中名字在{}范围内可见。
    - 猜测是C++语法设定{}。
